export type MoreSkillsTooltipProps = {
  additionalSkills: string[];
  count: number;
};

export type SkillBadgeProps = {
  skill: string;
};


export type Option = {
  label: string;
  value: string | number;
};
